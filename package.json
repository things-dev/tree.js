{
  "name": "tree-js",
  "version": "1.0.0",
  "type": "module",
  "author": "Yuki Shimizu",
  "main": "index.js",
  "description": "",
  "keywords": [
    "tree",
    "treemodel",
    "javascript",
    "node",
    "hierarchy",
    "structure"
  ],
  "license": "MIT",
  "scripts": {
    "prepare": "husky install",
    "build": "rm -rf ./dist/index.js && bun build ./src --outfile=./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "typecheck": "tsc --pretty --noEmit",
    "format": "biome format ./src --write",
    "lint": "biome lint ./src",
    "lint:fix": "biome lint --apply ./src"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.4.1",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "typescript": "^5.3.3"
  },
  "lint-staged": {
    "*.ts": "bun run format && bun run lint:fix"
  }
}
